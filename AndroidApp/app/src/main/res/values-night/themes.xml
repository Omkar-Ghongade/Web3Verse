<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WebVerse" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:statusBarColor">@color/darkblack</item>
    </style>

    <style name="Theme.WebVerse" parent="Base.Theme.WebVerse" >

        <item name="android:statusBarColor">@color/darkblack</item>
    </style>
</resources>